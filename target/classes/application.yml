spring:
  application:
    name: demo
  main:
    web-application-type: none
  http:
    client:
      factory: jdk
      connect-timeout: 10s
      read-timeout: 10m
  ai:
    retry:
      max-attempts: 1
    ollama:
      baseUrl: http://127.0.0.1:11434
      chat:
        options:
          model: qwen3:8b

milvus:
  host: localhost
  port: 19530
  collection-name: aibrainbox_knowledge
  dimension: 768

app:
  llm:
    timeout: 10m


logging:
  level:
    org.springframework.ai: INFO
